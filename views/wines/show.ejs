<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Wine Show</title>
  </head>
  <body>
    <header>
        <h1>Wine About It - This Wine</h1>
        <nav>
            <ul>
                <li>
                    <a href="/">Home</a>
                </li>
                <li>
                    <a href="/wines">Wine List</a>
                </li>
            </ul>
        </nav>
    </header>
    <main>
        <section>
            <h2>Posts About <%=wine.name%>
            </h2>
            <img src="<%=wine.image%>" alt="<%=wine.name%>" />
            <p>
                <%=wine.about%>
            </p>

            <ul>
              <% for(let i = 0; i < wine.posts.length; i++) { %>
								<li>
                  <p>Username: <%=wine.posts[i].name%></p>
                  <a href="/wines/<%=wine.posts[i].id%>">
                    <img src="<%=wine.posts[i].image%>"/>
                  </a>
                  <p>
                    <a href="/wines/<%=wine.posts[i].id%>">
                      About this Wine:
                    </a>
                    <%=wine.posts[i].about%>
                  </p>
                  <a href="/posts/<%=wine.posts._id%>/edit">Edit Posts</a>
                  <form action="/posts/<%=wine.posts._id%>?_method=DELETE" method="POST">
                    <input type="submit" value="Delete Post">
                  </form>
								</li>
							<% } %>
            </ul>

        </section>

    </main>
  </body>
</html>
